import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>Fitness App</h1>
      {/* Add other navbar links here */}
    </nav>
  );
};

export default Navbar;
